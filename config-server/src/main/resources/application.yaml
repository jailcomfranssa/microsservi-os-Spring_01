server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/jailcomfranssa/microsservicos-Spring_01
          search-paths: config-data
  application:
    name: config-service
